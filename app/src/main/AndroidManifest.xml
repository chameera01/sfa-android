<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ahmed.sfa">


    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"

        android:theme="@style/AppTheme.NoActionBar">
        <supports-screens android:requiresSmallestWidthDp="600" />

        <activity android:name=".activities.supportactivities.CheckIn" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.ahmed.sfa.activities.AddExtraCustomer"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".activities.AndroidDatabaseManager" android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".activities.AddCustomer" android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".activities.DisplayProductTableActivity" android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".activities.Home" android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".activities.StockView" android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".activities.PendingCustomer" android:windowSoftInputMode="stateHidden"/>
    </application>

</manifest>